# 変更履歴

このファイルには、プロジェクトのすべての重要な変更が記録されます。

## [v2.0.0] - 2026-05-01

税計算ロジックの全面刷新を含むメジャーリリース。明細単位で税計算→丸めを行ってから集計していた従来方式では、丸め誤差が累積して集計値がレシートの実額と一致しないケースがあった（4月の食費集計で 107 円差が判明したのが発端）。本リリースで、計算式を「1 取引内では税率ごとに SUM(税抜) → 税計算 → ヘッダーの TAX_ROUNDING_TYPE で丸め → 税率合算」「取引をまたぐ集計では既に整数化された値をそのまま SUM、追加丸めしない」に統一した。

### 破壊的変更

- **AMOUNT のセマンティクス**: 取引明細の `AMOUNT` は税抜額固定として扱う。`AMOUNT_INCLUDING_TAX` は UI / DB ともに残し、税込表示用の派生値とする
- **集計クエリの全面書き換え**: 明細レベルのサブクエリで税込/税抜を判定し、ヘッダー単位で丸めた値を集計する方式へ。重複計上バグと累積誤差を解消（commit `dc720521`）

### 新機能

- **ヘッダー TOTAL_AMOUNT の自動再計算**（commits `984987d5` `d600a2a4`）: 明細編集・ヘッダー編集後にプロンプトを表示し、「OK で自動値で上書き／キャンセルで手動値維持」の選択を提供
- **税方式変更時のリアルタイムプレビュー**（commit `31cadb09`）: ヘッダー編集モーダルで税方式（税抜/税込/丸め方式）を変更すると、合計欄が即座に再計算されて表示される（フロント JS 純関数 + Jest テスト）
- **一括再計算 + ワンクリックロールバック**（commits `83d90e78` `f7d61a13`）: ダッシュボードに「データメンテナンス」セクションを追加。実行前に DB ファイルを自動バックアップし、不本意な結果になった場合は 1 クリックで元の状態に戻せる
- **パターンマッチ式 recalc**（commit `b6cbab91`）: ユーザーが入力した `TOTAL_AMOUNT` を正として、4 パターン（税抜 floor / half-up / ceil + 税込）を総当たりで検証。一致するパターンが見つかれば `TAX_ROUNDING_TYPE` / `TAX_INCLUDED_TYPE` を補正、一致しない場合のみ `TOTAL_AMOUNT` を上書き。詳細な変更ログを表示
- **口座別残高スナップショット**（commit `f888c8bc`）: ダッシュボード上部に口座ごとの残高パネルを追加。元データとの突き合わせ検証に使える

### 修正

- **ceil 丸めで 1 円欠落**（commit `7bd04550`）: SQLite の `CAST(-x/100 AS INTEGER)` イディオムが trunc-toward-zero の影響で実際は floor になっていた。`(x + 99) / 100` への書き換えで ceil として正しく動作するよう修正。Rust 側 / JS 側は最初から正しかったので影響範囲は SQL のみ
- **SQLite ATTACH / UPDATE / DETACH の同一接続化**（commit `7d844f7c`）: 一括再計算で別接続が DETACH を見られず NOT FOUND になる不具合を解消
- **ダッシュボードカラムのスクロール**（commit `6e27e3f2`）: コンテンツがビューポートを超えるときにカラム内でスクロールできるよう修正

### 開発体験 / メンテナンス

- **@tauri-apps/cli を ^2.11.0 に更新**（commit `d354b689`）

### バージョン方針

`AMOUNT` のセマンティクス変更は API レベルの破壊的変更に相当するため MAJOR を上げて v2.0.0 とした。データベースのスキーマ変更はないので、既存ユーザーは特別な移行作業なしにアップデート可能。集計値が以前と異なる場合があるが、これは丸め誤差の修正によるもので、新しい値の方がレシート実額と一致する。

---

## [v1.2.2] - 2026-04-30

### 修正

- **明細一覧画面のフィルタ**: 中分類（CATEGORY2_CODE）・小分類（CATEGORY3_CODE）によるフィルタが効かず、親カテゴリの全件が返されてしまう不具合を修正
  - `get_transactions` 関数で引数が `let _ = ...` で破棄されていたバグ
  - `EXISTS` サブクエリでヘッダー行数を維持しつつフィルタを適用
  - 空文字列は「フィルタなし」として扱う仕様に統一
  - 回帰防止用のテストを追加

### CI / セキュリティ

- **GitHub Actions ワークフロー**: `feature-branch-test.yml` に最小権限（`contents: read`）を明示
- **テストカバレッジ成果物の整理**: `res/tests/coverage/` 配下の自動生成ファイルがリポジトリに紛れ込んでいたため除去
- **Code Scanning ベースライン整備**: 既存アラート 26 件を判定・整理（誤検知の dismiss と本物の修正）
- **Dependabot アラート整理**: GHSA-cq8v-f236-94qc（rand 間接依存）を `tolerable_risk` で dismiss

### ドキュメント

- セッション初期コンテキスト（CLAUDE.md / `.github/copilot-instructions.md`）を圧縮
- 税計算ロジックリファクタプランを追加（v2.0.0 へ向けた設計文書）
- プロジェクト固有スラッシュコマンド `/i18n-add` と `/release` を追加
- リリース手順書のスクリプト名誤記を修正（`pre-release-check.sh` → `check-release.sh`）

---

## [v1.2.1] - 2026-04-24

### 修正

- **口座管理画面**: NONE口座（内部用「未指定」口座）を一覧から非表示に変更
  - ユーザーが誤ってNONE口座を編集できてしまう問題を防止
- **明細管理画面**: 明細追加モーダルのフォーカス改善
  - モーダル表示時に品目名フィールドへ自動フォーカス
  - スクロール位置のずれを防止（`preventScroll`対応）

---

## [v1.2.0] - 2026-04-19

### 新機能

- **予定入出金（IS_SCHEDULED）**: 入出金データに「予定」フラグを追加
  - 予定入出金の登録・確定（予定→実績への変換）
  - 一覧画面で予定と実績を視覚的に区別（背景色・バッジ表���）
  - デフォルトで予定を除外、チェックボックスで含める切替
- **集計機能の予定入出金フィルター**: 全5種（月次・日次・週次・期間・年次）に対応
- **集計の未来日付制限撤廃**: 予定入出金の集計に対応
- **週次集計の集計対象週表示**: 基準日選択時にリアルタイムで対象週範囲を表示
- **集計金額の色分け**: 収入（緑）・支出（赤）を視覚的に区別

### 国際化

- 集計エラーメッセージのi18n化（英語ハードコードから日英対応へ）
- 予定入出金関連の日英リソース追加（34エントリ）

### テスト

- バックエンド: 248テスト
- フロントエンド: 216テスト

---

## [v1.1.2] - 2026-04-16

### セキュリティ

- **randクレートのアップグレード**: v0.8 → v0.9（GHSA-cq8v-f236-94qc対応）
  - Dependabot Alert #13への対応
  - `thread_rng().gen()` → `rand::random()` へのAPI移行

---

## [v1.1.1] - 2026-01-26

### セキュリティ・安定性向上

- **依存関係の更新**: 76パッケージを最新版に更新
- **未使用コードの整理**: デッドコードの削除と警告の解消
- **CI/CD改善**: リリースビルド完全性チェックの追加
  - `test-release-build`ジョブをワークフローに追加
  - `scripts/check-release.sh`スクリプトを追加

### テスト

- バックエンド: 201テスト
- フロントエンド: 599テスト
- 合計: 800テスト

---

## [v1.1.0] - 2026-01-07

### 追加

- **ダッシュボード機能**: Chart.jsによるグラフ表示
  - 月別収支推移グラフ
  - カテゴリ別円グラフ

---

## [未リリース] - 2025-11-10

### 追加

**feat(i18n): CATEGORY2_I18NとCATEGORY3_I18Nに日本語リソースを追加**

#### データベース初期化:
- CATEGORY2_I18Nテーブルに日本語リソースを追加（ユーザーごとに20件）
- CATEGORY3_I18Nテーブルに日本語リソースを追加（ユーザーごとに126件）
- `res/sql/default_categories_seed.sql`を更新し、日本語と英語の両方のリソースを含めた
- I18Nエントリー総数: CATEGORY2_I18N 40件(ja+en)、CATEGORY3_I18N 252件(ja+en)

#### 一貫性の向上:
- すべてのカテゴリI18Nテーブル（CATEGORY1_I18N、CATEGORY2_I18N、CATEGORY3_I18N）で一貫したバイリンガルサポートを実現
- データベース初期化時に日本語リソースが自動投入されるようになった
- 費目管理UIで日本語名が正しく表示されるようになった

### 修正

**fix(account): AccountとAccountTemplate構造体にsqlxマッピング属性を追加**

#### バグ修正:
- `src/services/account.rs`の`AccountTemplate`構造体に`#[sqlx(rename_all = "SCREAMING_SNAKE_CASE")]`属性を追加
- `src/services/account.rs`の`Account`構造体に`#[sqlx(rename_all = "SCREAMING_SNAKE_CASE")]`属性を追加
- SQLiteの大文字カラム名とRustの小文字フィールド名のマッピング問題を修正
- ユーザー登録時の「no column found for name: template_id」エラーを解決

---

## [初期リリース] - 2025-10-25

### 追加 - Commit 1 (8e20ede3b7dfd7cdd3c5bec0590bbca15e252f40)

**docs: ドキュメントを言語別に整理 & consts.defをconsts.rsにリネーム**

#### ドキュメントの整理:
- 日英混在のドキュメントを言語別ディレクトリに分離
- `docs/ja/`に日本語版を配置
- `docs/en/`に英語版を配置
- 将来的な多言語対応に備えた構成

#### ドキュメントの改善:
- テストセクションを「実装済み」と「今後の実装予定」に明確に分離
- 非選択状態の説明を正確に修正（グレーの枠線 #666）
- 「視力の弱い方（ロービジョンの方）」の表記に統一
- より柔らかい表現に修正

#### ファイルリネーム:
- `res/consts.def` → `res/consts.rs` (Rustの命名規則に準拠)

#### ドキュメントポリシー:
今後、新しい言語のドキュメントは対応する言語ディレクトリに配置
- 例: 中国語 → `docs/zh/`
